<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
	elementFormDefault="qualified" >

	<xs:include schemaLocation="meteo1.xsd"></xs:include>
	<xs:include schemaLocation="meteo2.xsd"></xs:include>

	<xs:element name="racine" >

		<xs:complexType>

			<xs:all>
				<xs:element ref="releveJournalier" />
				<xs:element ref="listeDeMeteo" />
			</xs:all>

		</xs:complexType>

		<xs:keyref name="metarKeyRef" refer="metarKey">
			<xs:selector xpath="releveJournalier/releve"></xs:selector>
			<xs:field xpath="@metar"></xs:field>
		</xs:keyref>
		 
	</xs:element>

</xs:schema>